'use client'

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '../ui/card'

export default function CounterFeature() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Counter (Placeholder)</CardTitle>
        <CardDescription>
          This is a placeholder counter component
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Counter functionality is not implemented.</p>
      </CardContent>
    </Card>
  )
}